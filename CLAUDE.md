@AGENTS.md

## Claude Notes

In the Smarty App managed-fork lane, follow the managed-fork update flow in
AGENTS.md: Smarty App's `omp-fork-agent-goals` package owns sync/build/link,
and a Paul-requested update lands end to end — lease-guarded push to fork
`main` plus dev1 deployment. Never push to `upstream` or rewrite published
OMP history.
